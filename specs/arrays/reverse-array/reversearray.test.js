//
// 1. **Task: Reverse an Array**
// - **Input:** `[1, 2, 3, 4, 5]`
// - **Output:** `[5, 4, 3, 2, 1]`

function reverseArray(nums) {
  // code goes here
}

// unit tests
// do not modify the below code
test.skip("reverse array", function () {
  const nums = [1, 2, 3, 4, 5];
  reverseArray(nums);
  expect(nums).toEqual([5, 4, 3, 2, 1]);
});
